bytardag.se
===========

To update a container do `docker-compose -f docker-compose.production.yml up -d --no-deps --build <service_name>`.

Remember to update both `web` and `worker` when doing upgrades.
